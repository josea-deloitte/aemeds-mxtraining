// add delayed functionality here

/*
 * Third-party martech from the original site belongs here so it never
 * competes with page rendering: Adobe Launch (launch-*.min.js), the
 * cookie consent manager (uc.js / CookieConsent), the HUMAN bot-detection
 * snippet (pgi.js), and the data layer bootstrap. Load them with
 * loadScript() from aem.js once the respective accounts/environments
 * are configured, e.g.:
 *
 * import { loadScript } from './aem.js';
 * await loadScript('https://assets.adobedtm.com/.../launch-XXXX.min.js', { async: true });
 */
